<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forget Password</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link id="mainStyle" rel="stylesheet" href="/userAssets/style-otp.css">
</head>
<body>
    <div class="verification-box">
        <h2>Forget Password</h2>
        <% if (locals.message && message.length> 0) { %>
            <div id="alertMessage" class="alert alert-danger">
                <%= message %>
            </div>
        <% } %>
        <form action="/forget" id="otpForm" method="post">
            <div class="form-group">
                <label for="otp">Enter registered Mail ID</label>
                <input type="text" class="form-control" id="otp" name="email" placeholder="Email" required>
            </div>
            
            <button type="submit" class="btn" id="verifyBtn">Send Reset Link</button>
        </form>
    </div>        
</body>
</html>
